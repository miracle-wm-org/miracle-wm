[package]
name = "miracle-plugin"
version = "0.1.0"
edition = "2024"
description = "Rust bindings for the miracle-wm plugin API"
license = "GPL-3.0-or-later"
homepage = "https://github.com/miracle-wm-org/miracle-wm"
repository = "https://github.com/miracle-wm-org/miracle-wm"
documentation = "https://docs.miracle-wm.org/"

[package.metadata.docs.rs]
targets = ["wasm32-unknown-unknown"]

[lib]
crate-type = ["rlib"]

[features]
default = ["configure"]
configure = ["dep:serde_json"]
# Regenerate src/bindings.rs from plugin.h. Requires libclang and Mir's development headers.
regen-bindings = ["dep:bindgen"]

[dependencies]
bitflags = "2"
glam = "0.29"
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", optional = true }

[build-dependencies]
bindgen = { version = "0.71", optional = true }
